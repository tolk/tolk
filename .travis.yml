language: ruby

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - bundle exec rake db:migrate RAILS_ENV=test
  - bundle exec rake
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1

env:
  - RAILS_VERSION="~> 4.0.0"
  - RAILS_VERSION="~> 4.1.0"
